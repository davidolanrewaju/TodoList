//Create type for Modal component props for cancelling modal view
type ModalComponentProps = {
  modalVisible: boolean;
  setModalVisible: (modalVisible: boolean) => void;
};

export default ModalComponentProps;
